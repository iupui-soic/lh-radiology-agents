{
  // Minimal Orthanc config for the LH-Radiology dev stack.
  //
  // Mount target MUST be /etc/orthanc/orthanc.json exactly. The orthancteam image
  // only reads that specific filename at boot; arbitrary *.json files are NOT
  // merged (the earlier /etc/orthanc/lh-radiology.json mount was silently ignored
  // -- PythonScript never registered and OnStableStudy never fired; !107 review).
  // This file therefore replaces the image's default orthanc.json, so any setting
  // we need for the stack (DICOMweb, storage paths, HTTP, plugin config) has to
  // be declared here rather than inherited.
  "Name": "lh-radiology",

  // Dev only: no HTTP auth so the OHIF viewer (proxied same-origin) can query freely.
  "AuthenticationEnabled": false,
  "RemoteAccessAllowed": true,

  // Enable DICOMweb (QIDO-RS / WADO-RS) — this is what OHIF consumes.
  "DicomWeb": {
    "Enable": true,
    "Root": "/dicom-web/",
    "EnableWado": true,
    "WadoRoot": "/wado"
  },

  // Persistence paths declared explicitly (@sunbiz's !107 review). Since this
  // file replaces the image's default orthanc.json rather than sitting beside it,
  // we cannot rely on the built-in default matching the compose volume mount
  // (`orthanc-db:/var/lib/orthanc/db`). Without these two settings a future
  // Orthanc image change could fall back to a relative path that the volume does
  // not cover, and studies would quietly not survive `docker compose restart
  // orthanc`. Belt-and-suspenders.
  "StorageDirectory": "/var/lib/orthanc/db",
  "IndexDirectory": "/var/lib/orthanc/db",

  // OnStableStudy -> orchestrator webhook (integrations/orthanc-plugin/orthanc_stable_study.py).
  // The orthancteam image ships the Python plugin built-in and auto-enables it as soon as
  // a PythonScript setting is present. The plugin reads ORCH_WEBHOOK_URL from the environment
  // (set on the orthanc service in docker-compose.yml).
  "PythonScript": "/etc/orthanc/orthanc_stable_study.py",
  "PythonVerbose": false
}
