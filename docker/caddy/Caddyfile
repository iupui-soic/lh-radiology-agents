# The #75 hosted-showcase front door: ONE TLS terminator for every radiologist-facing surface.
# Pairs with docker-compose.tls.yml, which rebinds every other host port to 127.0.0.1 -- off-box,
# only this proxy's 80/443 exist, and nothing is served plaintext.
#
# Knobs (all env, all defaulted for a localhost rehearsal):
#   DEMO_DOMAIN          public name of the demo host (default: localhost).
#   DEMO_TLS             "internal" (default) = Caddy's own CA -- used for rehearsals. On a
#                        public FQDN set it to an operator email and Caddy switches to
#                        ACME/Let's Encrypt automatically. Applies ONLY to the public site
#                        block below; the in-cluster https://caddy name is always minted by
#                        the internal CA regardless (see that block's comment).
#   DEMO_PROXY_USER / DEMO_PROXY_PASS_HASH
#                        proxy-level login for the viewer/worklist origin (#75 item 3 accepts
#                        proxy-level auth for the demo). Generate a hash with:
#                          docker run --rm caddy:2.8 caddy hash-password -p '<password>'
#                        The default below is the REHEARSAL password "change-me" -- always
#                        override it on a reachable host.

(showcase_routes) {
	# The radiology module's vendor assets (jquery, datatables, moment, ...) are MISSING from
	# this omod build -- the module 404s them and every RIS dashboard page dies on
	# "jQuery is not defined" (found in the M4 recording dry run). Until the omod bundles them
	# again, serve pinned copies from the overlay (docker/caddy/radiology-vendor, mounted at
	# /srv/radiology-vendor). Must precede the /openmrs/* handle.
	handle /openmrs/moduleResources/radiology/vendor/* {
		uri strip_prefix /openmrs/moduleResources/radiology/vendor
		root * /srv/radiology-vendor
		file_server
	}

	# RIS + fhir2. Deliberately NOT behind the proxy login: OpenMRS enforces its own accounts
	# (radiologists sign under their own identity, #75 item 4), and the agents' fhir2 calls on
	# this same route authenticate with FHIR2_BASIC_* against fhir2 itself. Stacking the proxy
	# login here would break both.
	handle /openmrs/* {
		reverse_proxy openmrs:8080
	}

	# Sign-off override surface (#57): must be reachable from a paged clinician's PHONE, so it
	# lives on the public route; the endpoint itself refuses anything without the override token
	# (503 with none configured). handle_path strips /ingress, so the orchestrator sees the same
	# /signoff/... paths it serves in-cluster.
	handle_path /ingress/* {
		reverse_proxy orchestrator:8090
	}

	# Critical-result ack surface (#79): like the override, tapped from a paged clinician's
	# PHONE, so it cannot sit behind the proxy login -- Basic carries ONE credential and the
	# ack endpoint needs it to be the tapper's own OpenMRS account (that is the attribution).
	# Safe outside the proxy login: the HMAC sig gate 403s any forged/enumerated link before
	# a password prompt is ever offered. Same strip as OHIF's nginx fan-out, so worklist-api
	# sees the /ack/... path it serves in-cluster.
	handle /reading-api/ack/* {
		uri strip_prefix /reading-api
		reverse_proxy worklist-api:8107
	}

	# Everything else is the viewer origin. OHIF's nginx already fans out same-origin to
	# /dicom-web (Orthanc) and /reading-api (worklist-api), so ONE proxy login guards the
	# viewer, the images, and the reading worklist together.
	handle {
		# Both values always arrive via the compose overlay's env (which owns the rehearsal
		# defaults -- Caddy's {$VAR:default} cannot apply because compose sets the vars).
		basic_auth {
			{$DEMO_PROXY_USER} {$DEMO_PROXY_PASS_HASH}
		}
		reverse_proxy ohif:80
	}
}

# Public name: rides DEMO_TLS (internal CA for rehearsals, ACME on a real FQDN).
https://{$DEMO_DOMAIN:localhost} {
	tls {$DEMO_TLS:internal}
	import showcase_routes
}

# In-cluster name the agents' FHIR2_BASE_URL rides (#30's transport condition). MUST stay a
# separate site block pinned to the internal CA: a shared block's single tls directive would,
# with DEMO_TLS set to an ACME email, ask a public CA to certify "caddy" -- an in-cluster-only
# name no CA can validate (rejectedIdentifier). Caddy is then left with no cert for that SNI
# and every agent's fhir2 hop dies with TLSV1_ALERT_INTERNAL_ERROR. SSL_CERT_FILE on the
# agents trusts the internal CA root, so internal is also the only issuer that works here.
https://caddy {
	tls internal
	import showcase_routes
}
